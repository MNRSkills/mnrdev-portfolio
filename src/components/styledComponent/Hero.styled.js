import styled from "styled-components";

export const StyledHeader = styled.header`
  .headshot {
    width: 100%;
    position: relative;
    .pitch {
        font-size: 4em;
        font-weight: 700;
      width: 50%;
      background-color: red;
      position: absolute;
      
    }
  }

  .headshot img {
    width: 100%;

  }
`;
